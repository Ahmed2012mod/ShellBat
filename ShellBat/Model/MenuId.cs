namespace ShellBat.Model;

public enum MenuId
{
    AppView,
    AppHistory,
    AppFavorites,
    AppInfo,
    AppInstance,
    AppThumbnailSize,
    AppSort,
    AppActions,

    MaxId // don't use, marker for the max enum value
}
